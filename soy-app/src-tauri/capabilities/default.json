{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and menubar windows",
  "windows": ["main", "menubar"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
